import { createElementId, IQAMap } from '@omaze/app';

export const displayName: string = 'WinnerPending';

export const WINNER_PENDING_QA: IQAMap = {
    WINNER_PENDING: createElementId(displayName, 'WinnerPending'),
};
