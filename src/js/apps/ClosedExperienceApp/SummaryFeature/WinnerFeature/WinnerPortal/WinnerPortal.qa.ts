import { createElementId, IQAMap } from '@omaze/app';

export const displayName: string = 'WinnerPortal';

export const WINNER_PORTAL_QA: IQAMap = {
    CONTAINER: createElementId(displayName, 'Container'),
};
