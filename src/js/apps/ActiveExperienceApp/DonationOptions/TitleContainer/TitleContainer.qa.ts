import { createElementId, IQAMap } from '@omaze/app';

export const displayName: string = 'TitleContainer';

export const TITLE_CONTAINER_QA: IQAMap = {
    CONTAINER: createElementId(displayName, 'Title'),
};
