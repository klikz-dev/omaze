import { createElementId, IQAMap } from '@omaze/app';

export const displayName: string = 'FameLink';

export const FAME_LINK_QA: IQAMap = {
    CONTAINER: createElementId(displayName, 'FameLink'),
};
