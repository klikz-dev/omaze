// this needs a standalone app so that it can load very quickly and get all the skeleton loading setup before trying to
// load other javascript
import './lib/skeleton-loading';

(function () {
    SDG.SkeletonLoading.init();
}())
