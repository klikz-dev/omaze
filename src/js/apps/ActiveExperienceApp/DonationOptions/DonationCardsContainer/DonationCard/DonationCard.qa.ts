import { createElementId, IQAMap } from '@omaze/app';

export const displayName: string = 'DonationCard';

export const DONATION_CARD_QA: IQAMap = {
    CONTAINER: createElementId(displayName, 'DonationCard'),
};
